### gitレポジトリ
- https://github.com/negigohan/codex.git

### ファイル編集作業を指示された場合、編集前に実施すること
- 編集対象のローカルのファイルと、gitにcommitされている最新のファイルを比較
- gitの最新ファイルが編集前のローカルのファイルより新しければ、gitの最新ファイルをpull
- 編集前のローカルのファイルのほうがgitの最新のものより新しければ、またはgitに存在しなければ、gitにcommit&push

### ファイル編集後に実施すること
- 編集したファイルのgitへのcommit&push

### コーディング作業の委譲
- プランモードでプランを作成後、新規のソースコードを作成する場合は必ずclaudeにソースコードを作成させること
- プランモードで作成したプランをそのままclaudeに渡せるように、プランには実装作業に必要な設計のみ記載すること
- 既存のソースコードを修正する場合は、ユーザーにcodexかclaudeのどちらが修正するか必ず確認すること
- claudeによるソースコード作成後、必ずcodexが動作確認を実施すること
- 動作確認に必要な環境（例: Playwright/jsdom など）が不足している場合は、Codexが不足ツールを導入してから動作確認を実施すること
- claudeの呼び出しは、claude --model Qwen3.6 --print --permission-mode bypassPermissions "プランモードで作成したプラン"、の形式で実行すること
- `--print` を使う場合は、プロンプト内で「実装完了後は即終了すること」「最終要約のみを短く出力すること」を必ず明示すること
- Codex経由でclaudeの呼び出しを実行する場合、`shell_command` の `timeout_ms` は原則 `1800000`（30分）をデフォルトとして扱うこと
- Codexはclaude実行中、対象ディレクトリを約1分間隔で確認し、必要ファイルの生成・更新が完了していれば動作確認に移行してよい
- 生成物の作成を確認できた場合は、claudeの返答完了を待ち続けず、必要に応じてclaudeプロセスを手動終了してCodexの動作確認フェーズへ移行してよい
